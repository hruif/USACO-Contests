#include <iostream>
#include <vector>

using namespace std;

int N, K;
vector<vector<int>> edges;

int main() {
	cin >> N >> K;
	edges.resize(N);
	for (int i = 0; i < K; i++) {
		int c, p, a, b;
		cin >> c >> p >> a >> b;
		c--;
	}
}